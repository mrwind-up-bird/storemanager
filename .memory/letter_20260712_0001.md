# Letter to Myself (Session Handoff)

**Date:** 2026-07-12

> Predecessor: `.memory/letter_20260710_0001.md` (still untracked — it names exposed creds; keep it OUT of the public repo). This letter continues from there.

## 1. Executive Summary
* **Goal:** Operate the **already-LIVE** qrsm.store production deployment (v2 storemanager, multi-tenant Next.js on shared nyxcore host `root@46.224.105.254`, deploy dir `/opt/storemanager`). This session's unit of work: close the last backup-script deploy-drift gap and persist learnings.
* **Current Status:** ✅ **All work done and host-verified.** Nothing in-flight. Prod healthy on image `…:b441e38…`. No open PRs. Remaining items are user-deferred decisions, not pending actions.

## 2. The "Done" List (Context Anchor)
* **PR #14 merged + deployed + verified** (`ci/rsync-scripts-bundle`, squash `b441e38`): added `scripts/backup-prod-db.sh` to the `ci.yml` deploy `rsync` source list so the nightly backup script auto-deploys instead of being hand-`scp`'d. `-R` recreates the `scripts/` path, `-a` preserves the `100755` exec bit. **Only** the operator script ships; dev tooling under `scripts/` (bootstrap/seed/backfill/icons) stays off prod.
* **Runbook synced** (`docs/RUNBOOK-qrsm-store.md` `## Backups`): was stale ("authored but NOT yet installed / no automatic backups running") → now reflects live state (nightly 03:15 cron, verified) + the new auto-deploy path; kept crontab (re)install steps for a rebuilt host.
* **Host verification (2026-07-11):** `qrsm-web` healthy on `…:b441e38b99…`; `/opt/storemanager/scripts/backup-prod-db.sh` present, `-rwxr-xr-x`, **sha256 == repo**, fresh mtime (proves CI re-shipped it); cron `15 3 * * *` present; latest dump `qrsm-20260711-031501.dump` (cron fired that morning).
* **nyxCore learn:** created 2 insights on project `2a08de33-9da7-4ac8-a68b-c5ab3182f59e` — `ed06e06c` (`gh run watch` false-negative) and `5fb8b54b` (operator-script deploy drift).
* **Memory updated:** `memory/qrsm-store-deploy.md` open-follow-ups now points at PR #14 (merged).

## 3. The "Pain" Log (CRITICAL)
* **Tried:** background `gh run watch <id> --exit-status` to gate on deploy success.
* **Failed:** it printed **RED** (non-zero exit) for run `29153404648` even though `gh run view` said `conclusion: success` (4/4 jobs) and the host was healthy. It long-polls and drops non-zero on a transient hiccup during multi-minute runs.
* **Workaround:** treat `gh run view <id> --json status,conclusion,jobs` **+ direct host verification** (`docker inspect … Health.Status`, running image tag, artifact sha256) as authoritative. *Note: do NOT trust `gh run watch` exit code alone for long deploy runs — persisted as nyxCore insight `ed06e06c`.*
* **(Prior-session pain, still true):** workflow/subagent output about infra layers must be verified against live state (the DNS-layer conflation bug). And `nyxcore_search` returns **v1** KB (Lightsail/port 3001/repo `q-records-storemanager`) — always verify v2 findings against the repo.

## 4. Active Variable State
* **Prod host:** `root@46.224.105.254` (`nyxcore-server-2`), dir `/opt/storemanager`, stack `qrsm-db`/`qrsm-web`/`qrsm-worker`/`qrsm-splash` on `nyxcore_nyxcore-net`. Running image `ghcr.io/mrwind-up-bird/storemanager:b441e38b99f72ab658e7bf29289a93da806edb13`.
* **Billing:** `BILLING_DRIVER=stripe` (**TEST mode**), real `sk_test_`/`whsec_` in `/opt/storemanager/.env`; webhook `https://admin.qrsm.store/api/billing/webhook` (unsigned POST → 400 verified).
* **Backups:** nightly `15 3 * * *` → `/var/log/qrsm-backup.log`, dumps in `/opt/storemanager/backups`, 14-day retention. Script now auto-deploys; **crontab is host state, NOT CI-managed**.
* **DNS/TLS:** IONOS DNS, wildcard `A *.qrsm.store → host`; TLS = per-host **TLS-ALPN-01** (no wildcard cert), enumerated `Host()` routers in shared `nyxcore-traefik-1`. Add a tenant = add `Host(<slug>.qrsm.store)` to the `qrsm-web` label + redeploy.
* **Local repo:** on `main` @ `b441e38`, clean. `.memory/letter_*.md` are untracked by design (public repo) — do NOT commit.

## 5. Immediate Next Steps
1. [ ] **(User-deferred)** Rotate transcript-exposed creds (Discogs/OpenAI keys + admin pw). Tracked in nyxCore action point `4999c069-713e-45e7-88e6-86f3248b833b`; rotation procedure in the runbook. Not started per user's explicit defer.
2. [ ] **(User-deferred)** DNS-01 wildcard cert resolver on the shared Traefik — blast radius = all 11 prod sites + needs IONOS creds (only Hetzner token on host). Deferred until tenant volume justifies a maintenance window.
3. [ ] **(Optional, future)** Any *new* operator script destined for the host must be added to the `ci.yml` rsync list (same pattern as PR #14), else it drifts.
4. [ ] **(Optional, hygiene)** The nyxCore KB for this project is v1-stale (repo link `q-records-storemanager`, v1 axiom docs). Consider re-indexing v2 if KB search accuracy starts to matter.
