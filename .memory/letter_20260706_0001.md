# Letter to Myself (Session Handoff)

**Date:** 2026-07-06, ~23:10 (Europe/Berlin)

## 1. Executive Summary
* **Goal:** Slice 6 (Onboarding + Superadmin + Billing) des q-records storemanager v2 per Subagent-Driven-Development (Spec→Plan→Implementation) fertigstellen: 13 Tasks ausführen, finaler Whole-Branch-Review, PR.
* **Current Status:** ✅ ABGESCHLOSSEN. Slice 6 als **PR #8 gemergt** in `main` (Merge-Commit `cebb1b4`). HD#3-Follow-up als **PR #9 offen** (`fix/onboarding-gating-helper`, Commit `89ba274`). Aktueller lokaler Branch: `fix/onboarding-gating-helper`.

## 2. The "Done" List (Context Anchor)
* **Alle 13 Tasks (T1–T13)** implementiert + task-reviewed (haiku/sonnet nach Komplexität; Personas Nemesis/Ipcha/Athena/Harmonia als Review-Linsen). Ledger: `.superpowers/sdd/progress.md`.
* **3 controller-entdeckte Fixes:** `be9a824` (Drift-Guard-Mock, seit T1 rot — subscriptions fehlte in SOUND_TENANT_ID_TABLES); `ca460fb` (KRITISCH: `next build`-Bruch, const-zod-Schemas aus `'use server'` in `schemas.ts` ausgelagert — createTenant/checkout/changePasswordSchema); `1860342` (E2E-DNS: `127.0.0.1`+Host statt `admin.localhost` für page.request).
* **Finaler 7-Dimensionen-Review** (RLS, Auth-Pipeline, Platform-Zone, Billing, Secrets, Architektur/Gating, UX/A11y): **kein Critical**, 4 Important → Sammel-Fixer `50de857` (dt. Abo-Status-Labels SUB_STATUS_DE in `src/lib/billing/types.ts`, Stripe-customerId-Self-Heal in `apply.ts`, bcrypt.compare-Timing-Guards, Superadmin-Nav auf `isSuperadmin`-Flag).
* **HD#3-Fix (PR #9):** `needsOnboarding(user, tenant)` in `src/lib/onboarding.ts` extrahiert (duplizierte Gating-Bedingung aus `(app)/layout.tsx` + `passwort/actions.ts` zusammengeführt), Unit-Test `tests/lib/onboarding.test.ts` 5/5.
* **nyxcore learn:** 3 Insights persistiert (projectId `2a08de33-9da7-4ac8-a68b-c5ab3182f59e`); Memory `sdd-final-review-build-gate.md` + MEMORY.md-Index geschrieben.
* **Gates final:** `pnpm test` 96 Dateien/714 Tests · typecheck 0 · lint 0 (2 bekannte fake.ts) · `docker compose build` · `pnpm e2e` 64/64.

## 3. The "Pain" Log (CRITICAL)
* **`'use server'` + `export const <zodSchema>`:** `next build` scheitert mit „Server Actions must be async functions", sobald eine Client-Komponente aus der Action-Datei importiert. **`tsc` und `vitest` fangen das NICHT — nur der echte Build.** Workaround: Schema in schwesterliche `schemas.ts` OHNE Direktive; Action importiert intern (kein Re-Export). *Nicht wieder const aus 'use server' exportieren.*
* **E2E gegen langlebigen Compose-DB-Container:** zeitfenster-basierte KPI-Tests (analytics.ts `collectionCount(period)`) werden durch akkumulierte Cruft nicht-deterministisch → falscher „Regression"-Alarm bei UNVERÄNDERTEM Code. Workaround: `docker compose down -v && up --build` VOR jedem E2E-Lauf. *Erst `git diff main..HEAD` prüfen, bevor man E2E-Fails als Regression deutet.*
* **Task-Reviews sind blind für Build-Regeln + cross-task-Regressionen** (nur Task-Diff, nie Voll-Suite/Build). Workaround: finaler Review MUSS `next build` + volle `pnpm test` + E2E fahren.
* **Session-Limits:** mehrfach getroffen (Opus-Subagenten). Ledger-SHAs machten Recovery möglich. *Ledger nach jedem Commit fortschreiben.*

## 4. Active Variable State
* **Compose-Stack läuft** (Image mit `50de857`, NICHT mit HD#3-Fix `89ba274`): db :55432, web :3000, worker, mailpit :8025. `.env.compose`: BILLING_DRIVER=fake, DISCOGS_DRIVER=fake, SEED_ADMIN_PASSWORD=E2eDevPassword1!, platform@qrecords.test.
* Frische Seed-Baseline nach `down -v`: demo 1 Collection/19 Purchases, freeshop 0/1 (maxRecords=2-Gate), vinylcave 0/16.
* E2E-Muster: `*.localhost` nur im Browser (`page.goto`); `page.request`/APIRequestContext braucht `127.0.0.1`+Host-Header.
* Scratch: `$CLAUDE_JOB_DIR/tmp/` (dev/compose/e2e-Logs, PR-Bodies, Review-Workflow-Script).

## 5. Immediate Next Steps
1. [ ] **PR #9 (HD#3) mergen** nach Review — oder Feedback einarbeiten. Ist reine verhaltens-erhaltende Extraktion + Unit-Test.
2. [ ] **Offene Human-Decisions** (nicht-blockierend, Follow-up-Tickets): HD#1 (platformLogoutAction ohne Session-Pflicht — Reviewer: akzeptabel) · HD#4 (`ankaufRecord` ohne kunde-Gate — pre-existing, C11-by-design). Falls Oliver eines angehen will: neuer Branch von `main`.
3. [ ] **Slice 7** (nächste vertikale Slice) via `superpowers:brainstorming` → Spec → Plan → SDD starten, wenn Oliver bereit ist.
4. [ ] Beim NÄCHSTEN finalen Review IMMER `docker compose build` + Voll-Suite + `down -v`-E2E einplanen (siehe Pain Log).
