# Letter to Myself (Session Handoff)

**Date:** 2026-07-10 · Session: qrsm.store production deploy

## 1. Executive Summary
* **Goal:** Deploy v2 storemanager to production on **qrsm.store** (shared nyxcore.cloud host `root@46.224.105.254`, behind Traefik), own Postgres, real integrations, via a git pipeline.
* **Current Status:** **LIVE & verified.** `https://admin.qrsm.store/login` (superadmin), `https://qrsm.store` (splash), valid Let's Encrypt certs, stack healthy on `/opt/storemanager`. Deploy pipeline (push → `main`) is green end-to-end.

## 2. The "Done" List (Context Anchor)
* Brainstormed → spec (`docs/superpowers/specs/2026-07-08-qrecords-v2-production-deploy-qrsm-store-design.md`) → plan (`docs/superpowers/plans/2026-07-08-qrecords-v2-production-deploy-qrsm-store.md`).
* Built via a parallel Workflow (haiku for config/docs, sonnet for code edits): `src/lib/platformAdmin.ts` (+ `tests/platform-admin.test.ts`), `scripts/bootstrap-prod.ts`, `src/lib/email/smtpOptions.ts` + `MAIL_SMTP_INSECURE` (`src/env.ts`, `src/lib/email/mailpit.ts`), `docker-compose.prod.yml`, `splash/index.html`, `.env.prod.example`, `deploy` job in `.github/workflows/ci.yml`, `docs/RUNBOOK-qrsm-store.md`. Dockerfile compiles `bootstrap-prod.cjs`.
* Full local gate green: typecheck, lint, **765 vitest**, `docker build`, `next build`, `docker compose config`.
* Branch `feat/deploy-qrsm-store` (based on `feat/v2-slice7-…`) → merged to `main` via **PR #11 + follow-up PR #12** → `main` CI deploy job ran green → superadmin `oli@qrsm.store` created.
* Server prepped: repo secrets `DEPLOY_SSH_KEY`/`DEPLOY_HOST`, dedicated ed25519 deploy key authorized on host, `/opt/storemanager/.env` filled (real Discogs+OpenAI keys, generated internal secrets, `BILLING_DRIVER=fake`).
* Recorded 2 nyxCore insights (`ac590029` planning, `4b7132c8` as-built+lessons) + updated project memory `qrsm-store-deploy.md`.

## 3. The "Pain" Log (CRITICAL — do not retry these)
* **Traefik uses `tlsChallenge` (TLS-ALPN-01), NOT DNS-01.** → No `*.qrsm.store` wildcard cert possible, and **`HostRegexp` routers don't auto-issue certs**. FIX: enumerate `Host(admin.qrsm.store)||Host(demo.qrsm.store)` + splash on apex/www. *Don't retry wildcard/HostRegexp on this Traefik without adding a DNS-01 resolver to `/opt/nyxcore/monitoring/traefik/traefik.yml`.*
* **GHCR image name:** nyxcore KB said `q-records-storemanager`; the real git remote is `mrwind-up-bird/storemanager`. Verify image refs against `git remote`, never the KB.
* **CI host-sanity `pnpm build` failed:** `/api/billing/webhook` pulls `@/env` at build-time page-data collection; `parseEnv` requires `DISCOGS_CONSUMER_KEY/SECRET` (no defaults) which were absent from that step's env. FIX: add placeholders (Dockerfile builder already had its own).
* **PR #11 merged EARLY** (before the last 2 fix commits) → `main` built/deployed stale config → follow-up PR #12. Always re-check what's on `main` before trusting the pipeline.
* **nodemailer type:** `Parameters<typeof nodemailer.createTransport>[0]` resolves to base `TransportOptions` (no `host`) → typecheck fails. Use `SMTPTransport.Options` (`import type … from 'nodemailer/lib/smtp-transport'`).
* **Test env-coupling:** importing any module that transitively loads `@/env` at the top of a spec throws (`parseEnv` on incomplete test env). FIX: keep pure logic in an env-free module (`smtpOptions.ts`), or dynamic-`import()` after env is set (see `tests/helpers/db.ts`).
* **Mail:** qrsm.store mail is IONOS-managed → sending `@qrsm.store` via the internal relay fails SPF/DKIM. Send from `noreply@nyxcore.cloud`.

## 4. Active Variable State
* **Server:** `root@46.224.105.254`, deploy dir **`/opt/storemanager`**, `.env` (server-only) — `ROOT_DOMAIN=qrsm.store`, `APP_PROTOCOL=https`, `APP_PORT=443`, `TRUST_PROXY=1`, `DISCOGS_DRIVER=http`(real), `EMBEDDINGS_DRIVER=http`(real OpenAI), **`BILLING_DRIVER=fake`**, `MAIL_FROM=noreply@nyxcore.cloud`. `qr_owner`/`qr_app` pw match the DB URLs; `ENCRYPTION_KEY` = 32 bytes.
* **DNS:** IONOS (`ui-dns.*`), wildcard `A * → 46.224.105.254` + apex.
* **Image:** `ghcr.io/mrwind-up-bird/storemanager:{sha,latest}`; pull auth = workflow `GITHUB_TOKEN` (`permissions: packages:read`).
* **Deploy key:** `/Users/oliverbaer/.claude/jobs/067301ee/tmp/qrsm_deploy` (ephemeral, in job tmp — regenerate if needed).
* **Superadmin:** `oli@qrsm.store` (pw in server `.env`). Traefik = `nyxcore-traefik-1` (v3.6.12).

## 5. Immediate Next Steps
1. [ ] Log into `admin.qrsm.store`, create the **`demo`** tenant (already routed → `demo.qrsm.store`).
2. [ ] Wire **real Stripe**: add `STRIPE_SECRET_KEY`(`sk_test_`)/`STRIPE_WEBHOOK_SECRET`(`whsec_`) to `/opt/storemanager/.env`, set `BILLING_DRIVER=stripe`, restart `qrsm-web` (webhook → `https://admin.qrsm.store/api/billing/webhook`).
3. [ ] Sync the **stale spec + runbook** to as-built (IONOS DNS, TLS-ALPN enumerate, mail-from-nyxcore, `/opt/storemanager`, GHCR-token pull, BILLING fake) — docs-only PR.
4. [ ] (Optional) Add an **IONOS DNS-01 resolver** to the shared Traefik → true `*.qrsm.store` wildcard so new tenants don't need a redeploy.
5. [ ] `pg_dump` backup cron for the `qrsm_db_data` volume.
6. [ ] Consider **rotating** the Discogs/OpenAI keys + admin password (they appeared in this session's transcript).
